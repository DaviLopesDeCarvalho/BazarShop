import React from 'react';
import { View, Text, Image, ScrollView, TouchableOpacity} from 'react-native';

export default function LoginScreem ({}) {
    return (
        <ScrollView>
            <View>
                <Text>Painel de Login</Text>
            </View>
        </ScrollView>
    )
}