import React from 'react';
import { View, Text, Image, ScrollView, TouchableOpacity} from 'react-native';

export default function CadastroScreem ({}) {
    return (
        <ScrollView>
            <View>
                <Text>Painel de Cadastro</Text>
            </View>
        </ScrollView>
    )
}